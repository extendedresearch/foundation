//! Nothing to test here: the tests are in `tests/cases.rs`, so this binary's
//! summary line counts zero and `tests/live.sh` checks the counts are summed.

/// The value the tests compare against.
pub fn one() -> u32 {
    1
}
